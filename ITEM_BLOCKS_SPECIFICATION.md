# 🎮 Tetris 아이템 블록 명세서

> **SE-tetris-team10** - 특수 아이템 블록 시스템 상세 문서

---

## 📋 목차
1. [개요](#-개요)
2. [아이템 블록 종류](#-아이템-블록-종류)
3. [아이템 생성 규칙](#-아이템-생성-규칙)
4. [기술 구현](#-기술-구현)
5. [게임 밸런스](#-게임-밸런스)

---

## 🎯 개요

### 아이템 모드란?
일반 테트리스 게임에 **5가지 특수 블록**을 추가하여 전략성과 재미를 극대화한 게임 모드입니다.

### 핵심 특징
- ✅ **5종류의 특수 아이템 블록**
- ✅ **3줄 클리어마다 1개 생성** (기본 규칙)
- ✅ **고유한 시각 효과 및 애니메이션**
- ✅ **점수 시스템 독립 설계**
- ✅ **난이도별 차등 적용**

---

## 🔮 아이템 블록 종류

### 1️⃣ AllClear Block (전체 클리어 블록)

#### 📊 기본 정보
| 속성 | 값 |
|------|-----|
| **블록 코드** | `2` |
| **모양** | 2x2 사각형 |
| **색상** | 특수 색상 (blindColor_1) |
| **회전 가능** | 예 |

#### 🎮 효과
```
보드 전체를 완전히 비웁니다 (벽 제외)
```

**동작 방식:**
1. 블록이 바닥에 고정되면 효과 발동
2. 전체 보드 깜빡임 애니메이션 (140ms)
3. 내부 영역(row 2~21, col 1~10) 모두 0으로 초기화
4. AllClear 후 자연스럽게 완성된 라인은 점수 없이 제거

#### 💰 점수
- **획득 점수**: 0점 (보너스 없음)

#### 📐 블록 구조
```java
shape = new int[][] {
    {2, 2},
    {2, 2}
};
```

#### 💡 전략 포인트
- 보드가 꽉 찼을 때 최대 효과
- 위기 상황 탈출용
- 고득점보다는 생존 전략

---

### 2️⃣ BoxClear Block (폭발 블록)

#### 📊 기본 정보
| 속성 | 값 |
|------|-----|
| **블록 코드** | `3` |
| **모양** | 3x3 십자가 (중앙만 활성) |
| **색상** | 특수 색상 (blindColor_1) |
| **회전 가능** | 예 |

#### 🎮 효과
```
중심점을 기준으로 5x5 영역을 폭발시킵니다
```

**동작 방식:**
1. 블록 고정 시 중심점(값 3) 위치 감지
2. 중심점 기준 ±2칸씩 5x5 영역 제거
3. 폭발 애니메이션 효과
4. 중력 적용 (남은 블록들이 아래로 떨어짐)
5. 중력 후 완성된 라인은 점수 없이 제거

#### 💰 점수
- **획득 점수**: 0점

#### 📐 블록 구조
```java
shape = new int[][] {
    {0, 0, 0},
    {0, 3, 0},
    {0, 0, 0}
};
```

#### 🔥 폭발 범위
```
□ □ □ □ □
□ □ □ □ □
□ □ ■ □ □  ← 중심점 (3)
□ □ □ □ □
□ □ □ □ □

■ = 중심점
□ = 제거 영역 (5x5)
```

#### 💡 전략 포인트
- 중앙 배치 시 최대 25칸 제거
- 벽 근처에서는 효과 감소
- 복잡한 블록 구조 해체용

---

### 3️⃣ OneLineClear Block (보너스 라인 블록)

#### 📊 기본 정보
| 속성 | 값 |
|------|-----|
| **블록 코드** | `4` |
| **모양** | 일반 블록 + 특수 셀 1개 |
| **색상** | 베이스 블록 색상 |
| **회전 가능** | 베이스 블록 따름 |

#### 🎮 효과
```
블록이 놓인 행을 즉시 제거하고 보너스 점수를 부여합니다
```

**동작 방식:**
1. 일반 블록 중 하나를 무작위로 선택
2. 해당 블록 내 한 셀을 값 `4`로 마킹
3. 블록 고정 시, 값 `4`가 있는 모든 행을 감지
4. 해당 행들을 즉시 제거 (깜빡임 애니메이션)
5. 위쪽 블록들이 아래로 이동

#### 💰 점수
```
점수 = calculateLineClearScore(지워진줄수 + 1, currentLevel)
```

**예시:**
- 값 `4`가 2개 행에 걸쳐있으면: `(2 + 1) = 3줄` 클리어 점수
- 1개 행만 있으면: `(1 + 1) = 2줄` 클리어 점수

#### 📐 생성 로직
```java
// 베이스 블록 복사
this.shape = copy2D(baseShape);

// 비어있지 않은 셀 중 하나를 무작위로 선택
cells.get(randomIndex)[0][1] = 4;
```

#### 💡 전략 포인트
- **보너스 점수 획득** (실제 클리어한 줄 + 1)
- 긴 블록(I-블록)에 적용되면 효과 극대화
- 멀티라인 클리어와 조합 가능

---

### 4️⃣ WeightBlock (무게추 블록)

#### 📊 기본 정보
| 속성 | 값 |
|------|-----|
| **블록 코드** | `6` (shape), `5` (효과) |
| **모양** | 4x4 역삼각형 |
| **색상** | 특수 색상 (blindColor_1, index 8) |
| **회전 가능** | ❌ **불가능** |

#### 🎮 효과
```
중력을 무시하고 아래로 관통하며 떨어집니다 (드릴 효과)
```

**동작 방식:**
1. 일반 이동: 좌우 이동만 가능, 회전 불가
2. 하드 드롭 불가 (거리 계산 0 반환)
3. 고정 시점: 드릴 애니메이션 시작
4. 드릴 효과:
   - 블록이 있는 각 열마다
   - 해당 블록 아래의 모든 셀을 제거
   - 바닥까지 관통
5. 중력 적용
6. 완성된 라인은 점수 없이 제거

#### 💰 점수
- **획득 점수**: 0점

#### 📐 블록 구조
```java
shape = new int[][] {
    {0, 0, 0, 0},
    {0, 0, 0, 0},
    {0, 6, 6, 0},
    {6, 6, 6, 6}
};
```

```
□ □ □ □
□ □ □ □
□ ■ ■ □
■ ■ ■ ■

■ = 무게추 블록 (값 6)
```

#### 🔧 드릴 효과 예시
```
적용 전:          적용 후:
□ ■ ■ □          □ ■ ■ □
□ ■ ■ □          □ □ □ □
■ ■ ■ ■   →      □ □ □ □
■ ■ ■ ■          □ □ □ □
■ ■ ■ ■          ■ □ □ ■
```

#### 💡 전략 포인트
- 높이 쌓인 블록 구조 파괴
- 중앙 열에 배치하여 최대 효과
- 회전 불가능하므로 배치 신중 필요

---

### 5️⃣ ScoreDouble Block (점수 2배 블록)

#### 📊 기본 정보
| 속성 | 값 |
|------|-----|
| **블록 코드** | (일반 블록에 플래그) |
| **모양** | 일반 블록과 동일 |
| **색상** | 특수 효과 |
| **회전 가능** | 예 |

#### 🎮 효과
```
해당 블록으로 라인 클리어 시 점수가 2배로 증가
```

**동작 방식:**
1. 일반 블록 중 하나를 ScoreDouble로 지정
2. 블록 고정 및 라인 클리어 시
3. 획득 점수 × 2
4. 특수 효과 표시

#### 💰 점수
```
최종 점수 = 기본 라인 클리어 점수 × 2
```

#### 💡 전략 포인트
- **고득점 노리기**
- 멀티라인 클리어와 조합 시 폭발적 점수
- 난이도 가중치와 중첩 적용

---

## 🎲 아이템 생성 규칙

### 기본 생성 조건
```
아이템 모드 활성화 시,
3줄 클리어마다 1개의 아이템 블록 생성
```

### 생성 로직
```java
boolean shouldSpawnItem = 
    (lineClearCount / 3) > itemGenerateCount;
```

**예시:**
- 3줄 클리어: 1번째 아이템 생성
- 6줄 클리어: 2번째 아이템 생성
- 9줄 클리어: 3번째 아이템 생성

### 아이템 선택 확률
```java
// 5가지 아이템 중 무작위 선택
int itemType = random.nextInt(5);

0 → AllClear
1 → BoxClear
2 → OneLineClear
3 → WeightBlock
4 → ScoreDouble
```

### 생성 위치
- **Next Block** 위치에 미리 표시
- 플레이어가 다음 블록을 미리 확인 가능

---

## ⚙️ 기술 구현

### 코드 구조

#### 아이템 블록 클래스
```
blocks/item/
├── AllClearBlock.java       (값 2)
├── BoxClearBlock.java        (값 3)
├── OneLineClearBlock.java    (값 4)
├── WeightBlock.java          (값 6 → 효과 5)
└── ScoreDoubleBlock.java     (특수 플래그)
```

#### 핸들러
```java
game/core/ItemBlockHandler.java
```
- `handleAllClear()` - 전체 클리어 처리
- `handleBoxClear()` - 폭발 처리
- `handleOneLineClear()` - 보너스 라인 처리
- `handleWeightBlock()` - 드릴 처리
- `handleScoreDouble()` - 점수 2배 처리

#### 애니메이션
```java
game/core/AnimationManager.java
```
- `startAllClearAnimation()` - 전체 깜빡임
- `startBoxClearAnimation()` - 폭발 효과
- `startLineClearAnimation()` - 라인 제거 효과
- `startWeightAnimation()` - 드릴 효과

### 블록 값 체계

| 값 | 의미 | 처리 |
|----|------|------|
| `0` | 빈 칸 | - |
| `1` | 일반 블록 | - |
| `2` | AllClear | 전체 클리어 |
| `3` | BoxClear | 5x5 폭발 |
| `4` | OneLineClear | 라인 보너스 |
| `5` | WeightBlock (효과) | 드릴 |
| `6` | WeightBlock (shape) | - |
| `10` | 벽 | - |

### 처리 흐름

```
블록 고정 (placeBlock)
    ↓
specialType 반환 (0~5)
    ↓
ItemBlockHandler.handleSpecialBlock()
    ↓
개별 처리 함수 호출
    ↓
애니메이션 시작
    ↓
효과 적용 (보드 수정)
    ↓
중력 적용 (필요시)
    ↓
라인 클리어 체크
    ↓
GameState 업데이트
    ↓
콜백 실행
```

---

## ⚖️ 게임 밸런스

### 아이템별 효과 등급

| 아이템 | 생존 | 점수 | 난이도 | 추천 상황 |
|--------|------|------|--------|-----------|
| AllClear | ⭐⭐⭐⭐⭐ | ⭐ | 쉬움 | 위기 탈출 |
| BoxClear | ⭐⭐⭐⭐ | ⭐ | 중간 | 구조 해체 |
| OneLineClear | ⭐⭐⭐ | ⭐⭐⭐⭐ | 쉬움 | 점수 획득 |
| WeightBlock | ⭐⭐⭐ | ⭐ | 어려움 | 높이 감소 |
| ScoreDouble | ⭐⭐ | ⭐⭐⭐⭐⭐ | 중간 | 고득점 |

### 점수 시스템

#### 일반 라인 클리어 점수
```java
baseScore = {
    1줄: 100,
    2줄: 300,
    3줄: 500,
    4줄: 800
}

최종점수 = baseScore × (레벨 + 1) × 난이도배율
```

#### 난이도 배율
```java
Normal: 1.0
Hard:   1.1
Easy:   0.9
```

#### OneLineClear 보너스
```
실제 1줄만 지워도 → 2줄 점수 부여
실제 2줄 지우면 → 3줄 점수 부여
```

### 밸런스 조정 요소

1. **생성 주기**: 3줄마다 (고정)
2. **무작위 선택**: 5가지 균등 확률
3. **점수 무효화**: AllClear, BoxClear, WeightBlock은 0점
4. **점수 보너스**: OneLineClear (+1줄), ScoreDouble (×2)

---

## 📝 개발 노트

### 구현 시 고려사항

1. **애니메이션 동기화**
   - 모든 아이템 효과는 애니메이션 완료 후 적용
   - `AnimationManager`가 게임 로직을 일시 정지

2. **보드 무결성**
   - 벽(값 10)은 절대 제거되지 않음
   - INNER 영역(2~21, 1~10)만 수정 가능

3. **중력 시스템**
   - BoxClear, WeightBlock 후 자동 적용
   - 위에서 아래로 순차 처리

4. **점수 계산 분리**
   - 아이템 효과 점수와 일반 클리어 점수 독립
   - `lastLineClearScore`로 추적

5. **이벤트 발행**
   - `ItemActivatedEvent` - 아이템 사용 알림
   - `LineClearedEvent` - 라인 클리어 알림
   - EventBus를 통한 느슨한 결합

---

## 🎓 사용 예시

### 게임 시작
```java
// 아이템 모드로 게임 시작
GameController controller = new GameController(view, true, 0);
controller.start();
```

### 아이템 생성 확인
```java
// 3줄 클리어 시
if ((lineClearCount / 3) > itemGenerateCount) {
    // 다음 블록을 아이템 블록으로 생성
    Block itemBlock = blockSpawner.spawnItemBlock();
}
```

### 효과 처리
```java
// 블록 고정 시
int specialType = engine.placeBlock(block, board, colorBoard);
itemBlockHandler.handleSpecialBlock(specialType, state, newState -> {
    // 효과 적용 완료 후 처리
    this.currentState = newState;
    spawnNewBlock();
});
```

---

## 📚 참고 문서

- `ItemBlockHandler.java` - 아이템 효과 구현
- `AnimationManager.java` - 애니메이션 시스템
- `BlockSpawner.java` - 블록 생성 로직
- `GameController.java` - 게임 흐름 제어

---

## 🏆 버전 정보

- **최초 작성**: v1.0
- **마지막 업데이트**: 2025-12-03
- **개발팀**: SE-tetris-team10

---

**이 명세서는 Tetris 아이템 모드의 공식 문서입니다.**
